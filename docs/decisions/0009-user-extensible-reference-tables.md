# 0009. User-extensible reference tables

- Status: Accepted
- Date: 2026-06-08

> **Stub — write-up pending.** A shared pattern for vocabularies the user curates:
> a small reference table, pre-seeded with sensible defaults, extensible from the
> UI without a schema migration (adding a value = inserting a row). Applies to
> `book_sources`, `genres` (auto-filled raw from Google Books for now;
> canonicalization deferred until there's a UI), and the future `places`
> (nationalities + book settings).

## Context

## Decision

## Consequences

## Alternatives considered

## Revisit when
